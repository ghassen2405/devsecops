<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>

<AnchorPane prefHeight="665.0" prefWidth="1006.0" style="-fx-background-color: linear-gradient(to bottom, #2E2E2E, #000000);" stylesheets="@styles.css" xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="Controllers.addproduitcontroller">

   <!-- Barre de navigation -->
   <HBox layoutX="0" layoutY="0" prefHeight="80.0" spacing="20" style="-fx-background-color: #1A1A1A; -fx-padding: 15; -fx-alignment: center-left;" AnchorPane.leftAnchor="0.0" AnchorPane.rightAnchor="0.0">
      <ImageView fx:id="logo" fitHeight="56.0" fitWidth="63.0" pickOnBounds="true" preserveRatio="true"/>
      <Button fx:id="homeBtn" onAction="#goToHome" text="Accueil" styleClass="nav-button"/>
      <Button fx:id="productsBtn" onAction="#goToProducts" text="Produits" styleClass="nav-button"/>
      <Button fx:id="claimsBtn" onAction="#goToReclamations" text="Réclamations" styleClass="nav-button"/>
      <Button fx:id="eventsBtn" onAction="#goToEvents" text="Événements" styleClass="nav-button"/>
      <TextField fx:id="searchField" promptText="Rechercher..." style="-fx-background-radius: 10; -fx-padding: 5; -fx-pref-width: 200px;"/>
      <Button text="🔍" style="-fx-background-color: black; -fx-text-fill: #FDD700; -fx-background-radius: 10;"/>
   </HBox>

   <!-- Contenu principal -->
   <VBox spacing="15" layoutX="50.0" layoutY="100.0" style="-fx-padding: 20; -fx-background-color: #181818; -fx-background-radius: 15; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.6), 10, 0, 0, 5);">
      <!-- Nom Section -->
      <HBox spacing="10">
         <Label text="Nom :" style="-fx-text-fill: white; -fx-font-size: 14px;"/>
         <TextField fx:id="nom" prefWidth="200.0" styleClass="input-field"/>
      </HBox>

      <!-- Description Section -->
      <HBox spacing="10">
         <Label text="Description :" style="-fx-text-fill: white; -fx-font-size: 14px;"/>
         <TextField fx:id="description" prefWidth="300.0" styleClass="input-field"/>
      </HBox>

      <!-- Catégorie Section -->
      <HBox spacing="10">
         <Label text="Catégorie :" style="-fx-text-fill: white; -fx-font-size: 14px;"/>
         <ComboBox fx:id="categorieComboBox" prefWidth="200.0" styleClass="input-field"/>
      </HBox>

      <!-- Prix Section -->
      <HBox spacing="10">
         <Label text="Prix :" style="-fx-text-fill: white; -fx-font-size: 14px;"/>
         <TextField fx:id="prix" prefWidth="100.0" styleClass="input-field"/>
      </HBox>

      <!-- Disponibilité Section -->
      <HBox spacing="10">
         <Label text="Disponibilité :" style="-fx-text-fill: white; -fx-font-size: 14px;"/>
         <TextField fx:id="disponibilite" prefWidth="100.0" styleClass="input-field"/>
      </HBox>

      <!-- Image Section -->
      <HBox spacing="10">
         <Label text="Image :" style="-fx-text-fill: white; -fx-font-size: 14px;"/>
         <ImageView fx:id="image" fitHeight="100.0" fitWidth="150.0" pickOnBounds="true" preserveRatio="true"/>
         <Button fx:id="browseImage" text="Parcourir Image" onAction="#browseImageAction" styleClass="action-button"/>
      </HBox>

      <!-- Buttons Section -->
      <HBox spacing="15" alignment="center">
         <Button fx:id="ajoutPromotion" text="Ajouter Promotion" onAction="#ajoutPromotion" styleClass="action-button"/>
         <Button fx:id="affich" text="Afficher" onAction="#Afficher" styleClass="action-button"/>
         <Button fx:id="ajout" text="Ajouter" onAction="#ajout" styleClass="action-button"/>
      </HBox>
   </VBox>

</AnchorPane>
